// Editor-only: teaches the JS language service about the plugin's virtual
// modules (virtual:pwa-register/react). Vite resolves the import at build time
// without this file; it just stops the editor flagging the import as missing.
/// <reference types="vite-plugin-pwa/react" />
/// <reference types="vite-plugin-pwa/info" />
